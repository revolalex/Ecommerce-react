export const setToken = (token) => ({
    type: 'SET_TOKEN',
    token: token
})

export const deleteToken = () => ({
    type: 'DELETE_TOKEN'
})

export const setUsers = (users) => ({
    type: 'SET_USERS',
    users: users
})

export const setID = (id) => ({
    type: 'SET_ID',
    id: id
})